# AI vs Human 文本偵測平台 - Vite 前端

使用 Vite + React + TypeScript + TailwindCSS 構建的現代化前端應用。

## 🚀 快速開始

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

應用將在 http://localhost:3000 啟動

### 建置生產版本

```bash
npm run build
```

### 預覽生產版本

```bash
npm run preview
```

## 📁 專案結構

```
frontend-vite/
├── src/
│   ├── pages/
│   │   ├── auth/
│   │   │   └── Login.tsx          # 登入頁面
│   │   ├── inference/
│   │   │   └── Inference.tsx       # 文本偵測頁面
│   │   └── dashboard/
│   │       └── ModelsDashboard.tsx # 模型管理頁面（Admin）
│   ├── App.tsx                     # 主應用組件
│   ├── main.tsx                    # 入口文件
│   └── index.css                   # 全局樣式
├── index.html                      # HTML 模板
├── vite.config.ts                  # Vite 配置
├── tailwind.config.js              # Tailwind 配置
└── package.json                    # 依賴配置
```

## 🔧 環境變數

在專案根目錄創建 `.env` 文件（可選）：

```env
VITE_API_URL=http://localhost:8000
VITE_TRAIN_API_URL=http://localhost:8001
```

## 📦 主要依賴

- **React 18** - UI 框架
- **React Router 6** - 路由管理
- **Vite** - 構建工具
- **TypeScript** - 類型安全
- **TailwindCSS** - 樣式框架
- **Chart.js** - 圖表庫
- **Axios** - HTTP 客戶端

## 🎯 功能

- ✅ 用戶認證（Admin/User）
- ✅ 文本偵測（五個模型）
- ✅ 視覺化圖表（長條圖、折線圖、雷達圖）
- ✅ 模型管理（Admin 專用）
- ✅ 訓練進度顯示
- ✅ 響應式設計

## 🔄 與 Next.js 版本的差異

1. **路由**: 使用 React Router 替代 Next.js Router
2. **構建**: 使用 Vite 替代 Next.js
3. **環境變數**: 使用 `import.meta.env` 替代 `process.env`
4. **啟動速度**: Vite 啟動更快，熱更新更迅速

## 🚀 優勢

- ⚡ **更快的啟動速度** - Vite 使用原生 ES 模組
- 🔥 **即時熱更新** - HMR 速度更快
- 📦 **更小的打包體積** - 更好的 tree-shaking
- 🛠️ **更簡單的配置** - 開箱即用

